v1.1.3
Add unit display in file and plot generation.

v1.1.2
Resolve the file generation failure if the timestamp index is a string type in the Data List Rule.

v1.1.1
Resolve the file generation failure if there is a data missing in source data when using "Group Merge" rule in Data List generation.

v1.0.3
1. Support custom frame data merging rules for Data List generation, including "Group Merge" rule to merge signals from different messages into the same line based on timestamp.
2. Layout improvements for Data File Mappings and Data List Rule editors, including options for ignoring title row and selecting merge rules.